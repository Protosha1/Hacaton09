"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from openrouter.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Any, Dict, List, Literal, Optional
from typing_extensions import NotRequired, TypedDict


MessagesBashToolResultBlockTypeContainerFileCitation = Literal[
    "container_file_citation",
]


class MessagesBashToolResultBlockFileTypedDict(TypedDict):
    container_id: str
    end_index: int
    file_id: str
    filename: str
    start_index: int
    type: MessagesBashToolResultBlockTypeContainerFileCitation


class MessagesBashToolResultBlockFile(BaseModel):
    container_id: str

    end_index: int

    file_id: str

    filename: str

    start_index: int

    type: MessagesBashToolResultBlockTypeContainerFileCitation


TypeOpenrouterBashToolResult = Literal["openrouter_bash_tool_result",]


class MessagesBashToolResultBlockTypedDict(TypedDict):
    r"""Output of a sandbox-executed `openrouter:bash` call from a prior assistant turn. Accepted on replay and dropped before the provider request — Anthropic has no equivalent block."""

    content: Dict[str, Any]
    tool_use_id: str
    type: TypeOpenrouterBashToolResult
    container_id: NotRequired[str]
    r"""The canonical container id the command ran under — the `{container_id}` for the Container Files API, reusable as a `container_reference` in later requests. Present on every sandbox-executed call, even when no files changed."""
    files: NotRequired[List[MessagesBashToolResultBlockFileTypedDict]]
    r"""Citations for the files the sandbox command created or modified, most-recently-touched first (at most 10). Retrieve them via the Container Files API."""


class MessagesBashToolResultBlock(BaseModel):
    r"""Output of a sandbox-executed `openrouter:bash` call from a prior assistant turn. Accepted on replay and dropped before the provider request — Anthropic has no equivalent block."""

    content: Dict[str, Any]

    tool_use_id: str

    type: TypeOpenrouterBashToolResult

    container_id: Optional[str] = None
    r"""The canonical container id the command ran under — the `{container_id}` for the Container Files API, reusable as a `container_reference` in later requests. Present on every sandbox-executed call, even when no files changed."""

    files: Optional[List[MessagesBashToolResultBlockFile]] = None
    r"""Citations for the files the sandbox command created or modified, most-recently-touched first (at most 10). Retrieve them via the Container Files API."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["container_id", "files"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
