"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .configurationupdatereasoning import (
    ConfigurationUpdateReasoning,
    ConfigurationUpdateReasoningTypedDict,
)
from openrouter.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
from pydantic import model_serializer
from typing import Literal
from typing_extensions import NotRequired, TypedDict


ConfigurationUpdateItemType = Literal["configuration_update",]


class ConfigurationUpdateItemTypedDict(TypedDict):
    r"""Changes reasoning effort from this point in the conversation onward without invalidating the prompt cache for the preceding items. Place it before the user message it should apply to; it stays in effect until another configuration update. Two adjacent configuration updates are rejected. Only supported by models that accept mid-conversation effort changes."""

    reasoning: ConfigurationUpdateReasoningTypedDict
    r"""Reasoning settings applied from this point in the conversation onward"""
    type: ConfigurationUpdateItemType
    id: NotRequired[Nullable[str]]


class ConfigurationUpdateItem(BaseModel):
    r"""Changes reasoning effort from this point in the conversation onward without invalidating the prompt cache for the preceding items. Place it before the user message it should apply to; it stays in effect until another configuration update. Two adjacent configuration updates are rejected. Only supported by models that accept mid-conversation effort changes."""

    reasoning: ConfigurationUpdateReasoning
    r"""Reasoning settings applied from this point in the conversation onward"""

    type: ConfigurationUpdateItemType

    id: OptionalNullable[str] = UNSET

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["id"])
        nullable_fields = set(["id"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m
