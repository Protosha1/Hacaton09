# Необязательно. По умолчанию фронт ходит на /api/... и dev-сервер Vite
# проксирует это на http://127.0.0.1:8000 (см. vite.config.js).
# Задайте VITE_API_URL, только если backend крутится на другом адресе/порту
# или вы делаете production build без прокси.
# VITE_API_URL=http://127.0.0.1:8000
